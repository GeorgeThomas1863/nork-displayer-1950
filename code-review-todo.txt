CODE REVIEW — ALL ITEMS RESOLVED
Last updated: 2026-03-01
=====================================

All 19 items (including API_PASSWORD) resolved on 2026-03-01.

=== CRITICAL SECURITY — RESOLVED ===

[1] RESOLVED — XSS via innerHTML in articles-return.js
    Fixed: buildArticleText now uses safe DOM insertion (createTextNode + createElement("br")).

[2] RESOLVED — Session fixation in auth-controller.js
    Fixed: req.session.regenerate() called before setting session flags in both
    authController and adminAuthController.

[3] SKIPPED — No rate limiting on auth routes
    Requires: npm install express-rate-limit --save
    Then: add authLimiter from express-rate-limit to both auth routes in routes/router.js.

[4] RESOLVED — adminPollingController hangs connections
    Fixed: Returns res.status(501).json({ error: "Not implemented" }).

[5] RESOLVED — No CSRF protection
    Fixed: sameSite changed from "lax" to "strict" in session-config.js.


=== HIGH — BUGS — RESOLVED ===

[6] RESOLVED — "oldest-to-newest" sort silently broken
    Fixed: sortDataReturn removed entirely. DB sort order is now preserved.

[7] RESOLVED — howMany uncapped
    Fixed: Math.min(+(howMany) || defaultDataLoad.X, 100) applied in
    articles.js, pics.js, and vids.js.

[8] RESOLVED — Wrong type field checked in runChangeButtonType
    Fixed: stateFront[`${typePrefix}Type`] === typeId in run.js.


=== MEDIUM — ARCHITECTURE — RESOLVED ===

[9] RESOLVED — Auth middleware returns 200 HTML on API POST routes
    Fixed: requireAuth and requireAdminAuth now return 401 JSON for non-GET requests.

[10] RESOLVED — Redundant dbConnect() creates leaked connections
    Fixed: Top-level await dbConnect() removed from db-model.js.
    Single dbConnect() call added to app.js before app.listen().

[11] RESOLVED — sortDataReturn duplicates and overrides DB sort
    Fixed: sortDataReturn removed from main-back.js entirely.

[12] RESOLVED — Admin data loads full collections with no limit
    Fixed: getAll(limit) now accepts a limit parameter; runGetAdminData calls getAll(500).

[13] RESOLVED — Static media served without auth
    Fixed: requireAuth middleware added to all three express.static media routes in app.js.


=== LOW — CODE QUALITY — RESOLVED ===

[14] RESOLVED — Duplicate element IDs on repeated DOM elements
    Fixed: All repeated IDs changed to className in articles-return.js, pics-return.js,
    vids-return.js, and collapse-display.js. CSS selectors updated in styles.css and
    media-styles.css.

[15] RESOLVED — Two conflicting click listeners per collapse header
    Fixed: Click listener removed from buildCollapseContainer. defineCollapseItems
    now handles both self-toggle and sibling-collapse in a single listener.
    pics-container.js updated to call defineCollapseItems.

[16] RESOLVED — Pervasive unnecessary async/await on synchronous functions
    Fixed: async removed from buildArticleTitle, buildArticleDate, buildArticleText,
    buildArticleTypeButtonItem, buildArticleParams, buildPicParams, buildVidParams,
    buildPicTypeButtonItem, buildPicContainerTitle, buildPicContainerDate,
    buildPicElementServer, buildVidTypeButtonItem, buildVidTitle, buildVidDate,
    buildVidElement, hideArray, unhideArray, defineCollapseItems.

[17] RESOLVED — Dead code and zero logging
    Fixed: middleware/auth-config.js deleted. src/kcna/state-kcna.js deleted.
    Commented-out imports removed from data-controller.js.
    console.error added in admin-back.js (both catch blocks) and admin-run.js.

[18] RESOLVED — Malformed input guard condition
    Fixed: Corrected to: if (!inputElement.value || isNaN(Number(inputElement.value)))


=== ADDITIONAL — RESOLVED ===

[API_PASSWORD] RESOLVED — API password not sent to scraper
    Fixed: apiPassword added to CONFIG in middleware/config.js.
    runAdminCommand now spreads apiPassword into axios.post payload.
